# 集群管理设计（ClusterManager 通用集群设施）

## 一、背景与定位

平台支持多节点部署，业务服务需要跨节点协同：

- **设备消息发送**：设备会话是本地状态（设备接入哪个节点，会话就在哪个节点），命令下发请求可能落在任意节点
- **网关启停**：管理端操作网关（启动/停止/重载）时，目标网关实例可能在其他节点
- **后续其他服务**：规则引擎、告警通知、定时任务等都可能需要跨节点调用

**ClusterManager 是通用集群设施，与具体业务无关**，只做三件事：节点管理、服务注册、服务获取（本地返回真实实例，远程返回代理，调用方无感知）。任何业务服务（设备发送、网关启停、后续其他）都可以通过 `register` 注册进来，通过 nodeId 获取并调用。

设备发送、网关启停等业务服务通过 `register` 注册、`getService` 获取，即可获得跨节点调用能力（业务侧使用示例见 [设备命令下发解决方案设计](./设备命令下发解决方案设计.md) 第四章）。

## 二、能力清单

| # | 能力 | 方法 |
|---|------|------|
| 1 | 获取当前节点 ID | `String getCurrentNodeId()` |
| 2 | 获取当前节点信息（ServerNode，含节点元数据） | `ServerNode getCurrentNode()` |
| 3 | 获取所有集群节点（含本节点） | `List<ServerNode> getClusterNodes()` |
| 4 | 按 nodeId 获取节点信息（不存在返回 null） | `ServerNode getServerNode(String nodeId)` |
| 5 | 注册服务到本节点（服务类型 + 实例） | `<T> void register(Class<T> serviceClass, T service)` |
| 6 | 通过 nodeId 获取服务实例（本地真实 / 远程代理） | `<T> Mono<T> getService(String nodeId, Class<T> serviceClass)` |
| 7 | 获取所有节点的指定服务（本节点真实 / 远程代理） | `default <T> Flux<T> getServices(Class<T> serviceClass)` |
| 8 | 获取本节点已注册的服务实例（未注册返回 null） | `<T> T getLocalService(Class<T> serviceClass)` |

> 不提供任何业务便捷方法（如 `getMessageSender`/`getGatewayOperator`）— ClusterManager 不知道也不关心业务服务是什么，业务方自己 `getService(nodeId, 自己的接口.class)`（或 `getServices(接口.class)` 遍历全部节点）即可。

## 三、ServerNode — 集群节点描述

```java
/** 集群节点描述 — 注册到 Hazelcast 分布式 Map，供节点发现 */
public class ServerNode implements Serializable {

    /** 节点 ID（Hazelcast Member UUID，集群内唯一） */
    private String nodeId;

    /** 节点主机地址 */
    private String host;

    /** 服务端口（用途标识，可空） */
    private Integer port;

    /** 注册时间 */
    private long registerTime;

    /** 扩展元数据（如承载的网关实例列表，预留） */
    private Map<String, Object> metadata;
}
```

## 四、ClusterManager — 接口（通用，无业务语义）

```java
/**
 * 集群管理器 — 每节点一个实例。
 * 节点管理 + 服务注册/获取：本节点服务注册到本地注册表；
 * 获取远程节点服务时返回动态代理，方法调用经 Hazelcast 转发到目标节点执行。
 * 通用设施，与具体业务无关 — 任何服务（设备发送、网关启停、后续其他）都可注册。
 */
public interface ClusterManager {

    /** ① 获取当前节点 ID */
    String getCurrentNodeId();

    /** ② 获取当前节点信息（ServerNode，含 host/注册时间/元数据等） */
    ServerNode getCurrentNode();

    /** ③ 获取所有集群节点（含本节点） */
    List<ServerNode> getClusterNodes();

    /** ④ 按 nodeId 获取节点信息 — 节点不存在/已退出返回 null */
    ServerNode getServerNode(String nodeId);

    /** 按 nodeId 获取 Hazelcast 实时成员（远程提交 submitToMember 用）— 节点已退出返回 null。
     *  ServerNode 不直接存 Member（Member 不可 Java 序列化，无法进分布式 Map），按 UUID 实时解析 */
    Member getServerMember(String nodeId);

    /** ⑤ 注册服务到本节点（服务类型 + 实例），其他节点经 getService 发现并调用 */
    <T> void register(Class<T> serviceClass, T service);

    /** ⑥ 通过 nodeId 获取服务实例 — 本节点返回真实实例，远程节点返回动态代理；未注册返回 empty */
    <T> Mono<T> getService(String nodeId, Class<T> serviceClass);

    /** ⑦ 获取所有节点的指定服务实例 — 本节点真实实例 + 远程节点代理（Flux 流式返回）。
     *  默认方法：由 getClusterNodes + getService 组合实现，实现类无需改动。
     *  未注册该服务的节点自动跳过（本节点 empty 过滤；远程节点代理调用时在目标节点报错，由调用方容忍） */
    default <T> Flux<T> getServices(Class<T> serviceClass) {
        return Flux.fromIterable(getClusterNodes())
                .map(ServerNode::getNodeId)
                .flatMap(nodeId -> getService(nodeId, serviceClass));
    }

    /** ⑧ 本节点已注册的服务实例 — 未注册返回 null（远程任务在目标节点执行时取本地服务用） */
    <T> T getLocalService(Class<T> serviceClass);
}
```

## 五、HazelcastClusterManager — 实现

```java
/**
 * ClusterManager Hazelcast 实现：
 * - 节点注册表：分布式 IMap（nodeId → ServerNode），构造时注册本节点，成员退出自动清理
 * - 本地服务注册表：本节点真实服务实例（ConcurrentHashMap，接口 → 实例）
 * - 远程代理：getService 远程节点时返回 JDK 动态代理，方法调用 → RemoteServiceInvoker
 *   提交到目标节点（IExecutorService）执行 → 结果回传
 */
public class HazelcastClusterManager implements ClusterManager {

    /** 远程调用超时（毫秒）— 需大于业务方法自身超时（如设备回复 10s），否则目标节点尚未完成即被截断 */
    private static final long DEFAULT_RPC_TIMEOUT = 15_000;

    private static final String NODE_REGISTRY_MAP = "cluster:node-registry";

    private final HazelcastInstance hazelcast;
    private final IMap<String, ServerNode> nodeMap;                   // 分布式节点注册表
    private final Map<Class<?>, Object> localServices = new ConcurrentHashMap<>();  // 本节点服务
    private final RemoteServiceInvoker remoteInvoker;                 // 远程调用器（代理方法调用 → 目标节点执行）

    // ===== 本节点管理器持有 =====

    /** 本 JVM 集群管理器 — 远程任务在目标节点执行时经此取本节点服务（构造时写入） */
    private static volatile HazelcastClusterManager CURRENT;

    /** 本 JVM 集群管理器 — 供 RemoteInvokeTask 在目标节点执行时获取本节点管理器 */
    public static HazelcastClusterManager getCurrent() {
        return CURRENT;
    }

    public HazelcastClusterManager(HazelcastInstance hazelcast) {
        this.hazelcast = hazelcast;
        this.nodeMap = hazelcast.getMap(NODE_REGISTRY_MAP);
        this.remoteInvoker = new RemoteServiceInvoker(this,
                hazelcast.getExecutorService("cluster-task"),
                DEFAULT_RPC_TIMEOUT);
        // 1. 写入本 JVM 静态持有器（远程任务在目标节点执行时经此取本节点管理器）
        CURRENT = this;
        // 2. 注册本节点到分布式节点注册表
        registerNode();
        // 3. 成员退出 → 清理该节点注册表项（节点异常下线兜底）
        hazelcast.getCluster().addMembershipListener(new MemberRemovedCleaner());
    }

    @Override
    public String getCurrentNodeId() {
        return hazelcast.getCluster().getLocalMember().getUuid().toString();
    }

    @Override
    public ServerNode getCurrentNode() {
        // 构造时已 registerNode() 写入分布式注册表，直接读取本节点项
        return nodeMap.get(getCurrentNodeId());
    }

    @Override
    public List<ServerNode> getClusterNodes() {
        return new ArrayList<>(nodeMap.values());
    }

    @Override
    public ServerNode getServerNode(String nodeId) {
        return nodeMap.get(nodeId);
    }

    /** 按 nodeId 获取 Hazelcast 实时成员（远程提交 submitToMember 用）— 节点已退出返回 null */
    @Override
    public Member getServerMember(String nodeId) {
        for (Member member : hazelcast.getCluster().getMembers()) {
            if (member.getUuid().toString().equals(nodeId)) {
                return member;
            }
        }
        return null;
    }

    @Override
    public <T> void register(Class<T> serviceClass, T service) {
        localServices.put(serviceClass, service);
    }

    @Override
    public <T> Mono<T> getService(String nodeId, Class<T> serviceClass) {
        if (nodeId == null || serviceClass == null) {
            return Mono.empty();
        }
        // 本节点 → 真实实例（未注册返回 empty）
        if (nodeId.equals(getCurrentNodeId())) {
            return Mono.justOrEmpty(getLocalService(serviceClass));
        }
        // 远程节点 → 动态代理
        return Mono.just(createRemoteProxy(nodeId, serviceClass));
    }

    // ===== 服务实例获取 =====

    /** 本节点真实实例 — 未注册返回 null（与远程分支 createRemoteProxy 对应的本地分支） */
    @Override
    public <T> T getLocalService(Class<T> serviceClass) {
        return serviceClass.cast(localServices.get(serviceClass));
    }

    // ===== 远程代理 =====

    /** 创建远程代理 — 方法调用经 RemoteServiceInvoker 转发到目标节点执行，调用方无感知 */
    private <T> T createRemoteProxy(String nodeId, Class<T> serviceClass) {
        return serviceClass.cast(Proxy.newProxyInstance(serviceClass.getClassLoader(),
                new Class<?>[]{serviceClass},
                (proxy, method, args) -> remoteInvoker.invoke(nodeId, serviceClass, method, args)));
    }

    /** 注册本节点到分布式节点注册表 */
    private void registerNode() {
        ServerNode node = new ServerNode();
        node.setNodeId(getCurrentNodeId());
        node.setHost(hazelcast.getCluster().getLocalMember().getAddress().getHost());
        node.setRegisterTime(System.currentTimeMillis());
        nodeMap.put(node.getNodeId(), node);
    }

    // ===== 成员退出清理 =====

    /** 成员退出 → 移除该节点的注册表项（节点异常下线兜底）。内部类，直接访问外部类 nodeMap */
    private class MemberRemovedCleaner implements MembershipListener {

        @Override
        public void memberAdded(MembershipEvent event) {
            // 成员加入无需处理 — 其注册表项由该节点自身 registerNode() 写入
        }

        @Override
        public void memberRemoved(MembershipEvent event) {
            String nodeId = event.getMember().getUuid().toString();
            nodeMap.remove(nodeId);
            // 本地服务注册表无需清理 — 成员退出后 getService 只会对存活节点创建代理
        }
    }
}
```

## 六、远程调用任务与序列化（Spring 传输类）

远程调用链路分三段：**RemoteServiceInvoker**（调用方：提交任务 + 还原结果）→ **RemoteInvokeTask**（目标节点：反射执行）→ **RemoteInvocationResult**（Spring 传输类：值/异常回传）。

传输层用 Spring 现成类（`org.springframework.remoting.support.RemoteInvocation` / `RemoteInvocationResult`，spring-context 自带，零新增依赖）：

- **RemoteInvocation**：可序列化方法载体（方法名/参数类型/参数值），替代手写的"方法名 + 参数 JSON"
- **RemoteInvocationResult**：值/异常回传；`recreate()` 有异常直接抛出（自动解包 InvocationTargetException）、无异常返回 value —— 替代手写异常重建
- **代价**：业务参数/返回值需实现 `Serializable`（Java 序列化跨节点传输）；当前无业务服务注册，零现有代码改动

### 6.1 RemoteServiceInvoker — 远程调用器（调用方侧）

```java
/**
 * 远程服务调用器 — 在目标节点调用服务方法并取回结果（调用方侧全流程）。
 * 流程：构造 RemoteInvokeTask（内部打包 RemoteInvocation 参数载体）→ 提交到目标节点（IExecutorService）执行 →
 * 阻塞等待结果 → 结果处理（recreate() 还原业务异常 / 取出返回值 → 按返回类型包装回 Mono/Flux）。
 * 供远程代理（createRemoteProxy）的 InvocationHandler 使用，业务方无感知。
 */
public class RemoteServiceInvoker {

    private final ClusterManager clusterManager;   // 集群管理器（节点注册表 + 远程调用器）
    private final IExecutorService executorService;   // 远程执行器（提交任务到目标节点执行）
    private final long timeoutMs;                           // 远程调用超时（毫秒）

    public RemoteServiceInvoker(ClusterManager clusterManager,
                                IExecutorService executorService,
                                long timeoutMs) {
        this.clusterManager = clusterManager;
        this.executorService = executorService;
        this.timeoutMs = timeoutMs;
    }

    /** 在目标节点调用服务方法并返回结果 — 远程代理 InvocationHandler 唯一入口 */
    public Object invoke(String nodeId, Class<?> serviceClass, Method method, Object[] args) {
        // 方法调用 → 构造远程任务（内部打包参数载体）→ 提交到目标节点执行 → 结果处理（handleResult）
        RemoteInvokeTask task = new RemoteInvokeTask(nodeId,
                clusterManager,
                serviceClass, method, args);
        return handleResult(submit(task, memberOf(nodeId)), method);
    }

    /** 提交任务到目标节点并阻塞等待结果 — 超时/中断/执行失败统一转为业务异常 */
    private RemoteInvocationResult submit(RemoteInvokeTask task, Member member) {
        try {
            return executorService.submitToMember(task, member).get(timeoutMs, TimeUnit.MILLISECONDS);
        } catch (TimeoutException e) {
            throw new BusinessException("远程调用超时: " + task.getNodeId());
        } catch (ExecutionException e) {
            throw new BusinessException("远程调用失败: " + e.getCause());
        } catch (InterruptedException e) {
            Thread.currentThread().interrupt();
            throw new BusinessException("远程调用被中断: " + task.getNodeId());
        }
    }

    /** 目标节点成员 — 经集群管理器按 nodeId 解析 Hazelcast 实时成员（ServerNode 不存 Member）；节点不存在/已退出则报错 */
    private Member memberOf(String nodeId) {
        Member member = clusterManager.getServerMember(nodeId);
        if (member == null) {
            throw new BusinessException("目标节点不可用: " + nodeId);
        }
        return member;
    }

    /** 结果处理（调用方侧）：recreate() 有异常直接抛出、无异常返回 value；成功后按方法返回类型包装回 Mono/Flux */
    private Object handleResult(RemoteInvocationResult result, Method method) {
        Object value;
        try {
            value = result.recreate();
        } catch (Throwable t) {
            throw asRuntimeException(t);   // RuntimeException/Error 原样抛出，checked 异常包装为 RuntimeException
        }
        return wrapReactive(value, method);
    }

    /** Mono/Flux 包装还原：远程已 block 出真实对象（Java 序列化还原），这里按方法返回类型还原为响应式形态 */
    private static Object wrapReactive(Object value, Method method) {
        Class<?> returnType = method.getReturnType();
        // Flux 方法：远程已 collectList → List 包装为 Flux
        if (Flux.class.isAssignableFrom(returnType)) {
            return Flux.fromIterable(value == null ? Collections.emptyList() : (List<?>) value);
        }
        if (Mono.class.isAssignableFrom(returnType)) {
            return Mono.justOrEmpty(value);
        }
        return value;
    }
}
```

### 6.2 RemoteInvokeTask — 远程调用任务（目标节点侧）

```java
/**
 * 远程调用任务 — 目标节点执行单元（Spring 传输类版）：
 * 构造时由 Method/args 拆解出可序列化的 RemoteInvocation（方法名/参数类型/参数值），
 * 目标节点执行：取本节点注册的服务实例 → 反射调用 → Mono/Flux block 出真实结果 → RemoteInvocationResult 回传。
 * 调用方侧的结果处理（recreate() 还原业务异常 / Mono/Flux 包装）在 RemoteServiceInvoker.handleResult。
 * 传输层用 Spring 现成类（RemoteInvocation/RemoteInvocationResult，spring-context 自带，零新增依赖），
 * 代价是业务参数/返回值需实现 Serializable（Java 序列化跨节点传输）。
 */
public class RemoteInvokeTask implements Callable<RemoteInvocationResult>, Serializable {

    private final String nodeId;                          // 目标节点
    private final transient ClusterManager clusterManager; // 本节点集群管理器（不参与跨节点序列化）
    private final Class<?> serviceClass;                  // 服务接口类型（Class 可序列化，目标节点按接口取本地注册实例）
    private final RemoteInvocation invocation;            // 可序列化方法载体：方法名/参数类型/参数值

    @Override
    public RemoteInvocationResult call() {
        // 目标节点：取本节点 ClusterManager（transient 字段不可达，经本节点管理器静态持有器）→ 取本地注册的真实服务
        ClusterManager manager = clusterManager;
        if (manager == null) {
            manager = HazelcastClusterManager.getCurrent();
        }
        if (manager == null) {
            return new RemoteInvocationResult(new IllegalStateException("本节点未装配集群管理器"));
        }
        Object service = manager.getLocalService(serviceClass);
        if (service == null) {
            return new RemoteInvocationResult(new IllegalStateException("目标节点未注册服务: " + serviceClass));
        }
        try {
            // Spring ReflectionUtils 定位方法（兼容接口方法）→ 反射调用（业务异常原样抛出）
            Method targetMethod = resolveMethod(service.getClass());
            Object result = ReflectionUtils.invokeMethod(targetMethod, service, invocation.getArguments());
            // 返回 Mono/Flux → block 出真实结果（业务完整执行发生在目标节点）
            if (result instanceof Mono) {
                result = ((Mono<?>) result).block();
            } else if (result instanceof Flux) {
                result = ((Flux<?>) result).collectList().block();
            }
            return new RemoteInvocationResult(result);
        } catch (Throwable t) {
            // 业务异常（如 BusinessException 设备离线/超时）随结果回传，调用方 recreate() 还原
            return new RemoteInvocationResult(t);
        }
    }
}
```

> 说明：目标节点经 `HazelcastClusterManager.getCurrent()`（本 JVM 静态持有器，bean 构造时写入）取本节点 ClusterManager — 任务经 Hazelcast 序列化到目标节点后 `transient clusterManager` 字段不可达，执行器线程脱离 Spring 无法注入，经本节点管理器静态持有器获取是唯一通道。

### 6.3 结果/异常回传 — Spring RemoteInvocationResult

```java
// org.springframework.remoting.support.RemoteInvocationResult（spring-context 自带，零新增依赖）
public class RemoteInvocationResult implements Serializable {

    private Object value;       // 返回值（成功时；null 则空）
    private Throwable exception; // 异常原样携带（失败时）

    public RemoteInvocationResult(Object value) { ... }          // 成功：返回值
    public RemoteInvocationResult(Throwable exception) { ... }   // 失败：异常原样携带

    public boolean hasException() { ... }     // 是否失败
    public Object getValue() { ... }          // 返回值

    /** 有异常直接抛出（自动解包 InvocationTargetException 取目标异常），无异常返回 value */
    public Object recreate() throws Throwable { ... }
}
```

传输规则：

- **参数/返回值/异常**：Java 序列化跨节点传输（`RemoteInvocation.arguments` / `RemoteInvocationResult.value` / `exception`）→ **业务对象需实现 Serializable**（`BusinessException extends RuntimeException` 天然可序列化 ✓）
- **异常还原**：调用方 `recreate()` 抛出的 `BusinessException`（如设备离线/超时）为 RuntimeException，原样还原为原业务错误；checked 异常包装为 `RuntimeException("远程调用失败", t)`
- **Mono/Flux**：目标节点 block/collectList 出真实对象 → 序列化还原 → 调用方按方法返回类型包装回响应式形态（值已是真实对象，无需再解泛型参数类型）

## 七、Bean 装配与配置

**依赖**（sa-base/pom.xml）：

```xml
<dependency>
    <groupId>com.hazelcast</groupId>
    <artifactId>hazelcast</artifactId>
    <version>5.3.7</version>   <!-- 5.3.x 最后版本，明确支持 Java 8（5.4 起要求 Java 11） -->
</dependency>
```

> 与 Redisson 无冲突（不同中间件；Redis 管缓存/业务数据，Hazelcast 管集群节点协同）。

**HazelcastClusterAutoConfiguration**（`cluster/config/`，仅 `iot.cluster.enabled=true` 时装配）：

```java
@Configuration
@ConditionalOnProperty(prefix = "iot.cluster", name = "enabled", havingValue = "true")
public class HazelcastClusterAutoConfiguration {

    /** Hazelcast 实例 — 集群名/成员列表来自配置；destroyMethod 优雅停机 */
    @Bean(destroyMethod = "shutdown")
    public HazelcastInstance hazelcastInstance(ClusterProperties properties) {
        Config config = new Config();
        config.setClusterName(properties.getHazelcast().getClusterName());
        config.getNetworkConfig().getJoin()
                .getTcpIpConfig().setEnabled(true)
                .setMembers(properties.getHazelcast().getMembers());
        return Hazelcast.newHazelcastInstance(config);
    }

    @Bean
    public ClusterManager clusterManager(HazelcastInstance hazelcast) {
        return new HazelcastClusterManager(hazelcast);
    }
}
```

**配置项**（application.yaml）：

```yaml
iot:
  cluster:
    enabled: false                      # 默认单机；多节点部署置 true
    hazelcast:
      cluster-name: smart-iot
      members:                          # TCP-IP 成员列表（生产）
        - 192.168.1.101:5701
        - 192.168.1.102:5701
```

> 本地集群验证：两个实例、enabled=true、不同端口（5701/5702）互加为成员即可模拟多节点。

## 八、时序图

### 8.1 服务注册与获取

```
节点 A                                    节点 B（目标）
  │ ClusterManager 启动（Hazelcast 组网）    │
  │ registerNode() → IMap 写入 nodeA         │ registerNode() → IMap 写入 nodeB
  │ register(业务服务接口, 本地实例)           │ register(业务服务接口, 本地实例)
  │────────────────────────────────────────▶│
  │ getService(nodeB, 业务服务接口)            │
  │   未命中本地注册表 → 创建动态代理          │
  │ 调用代理方法 → 提交 RemoteInvokeTask      │
  │────────────────────────────────────────▶│ 执行 task.call()
  │                                          │ → HazelcastClusterManager.getCurrent() 取本节点 ClusterManager
  │                                          │ → getLocalService(接口) → 真实实例
  │                                          │ → 反射调用方法 → 结果（Mono/Flux 则 block）
  │ ◀──── RemoteInvocationResult 回传 ───────│
  │ 代理 recreate() 还原异常/值 + Mono/Flux 包装 → 调用方无感知│
```

### 8.2 节点异常下线

```
节点 B 宕机
  → Hazelcast 成员退出事件
  → MemberRemovedCleaner.memberRemoved(nodeB)
  → nodeMap.remove(nodeB)           // 注册表清理
  → 后续 getService(nodeB, ...) 提交失败 → 调用方报"目标节点不可用"
```

## 九、异常与边界

| 场景 | 行为 |
|------|------|
| nodeId 不存在 / 节点已退出 | `memberOf(nodeId)` 提交失败 → 调用方报错（如"目标节点不可用"），由业务方决定提示语 |
| 远程调用超时 / 节点繁忙 | `get(DEFAULT_RPC_TIMEOUT)` 超时 → `TimeoutException` 回传；业务方按需放大超时（建议远程方法自身超时 < RPC 超时） |
| 目标节点业务异常 | 业务异常经 `RemoteInvocationResult` 原样携带回传，调用方 `recreate()` 直接抛出还原（`BusinessException` 为 RuntimeException 原样还原） |
| 参数/返回值序列化失败 | 业务对象需实现 `Serializable`（Java 序列化传输）；失败报错并记录日志 |
| 节点异常下线 | 成员监听清理节点注册表项；业务侧兜底见各业务设计（如设备位置 Redis 心跳） |
| 单机部署 | `iot.cluster.enabled=false` 不装配 Hazelcast，无 Hazelcast 运行时依赖 |

## 十、涉及文件清单

| 文件 | 操作 |
|------|------|
| `cluster/ClusterManager.java` | **新建（接口）** — 6 项能力：当前节点 ID / 当前节点信息 / 所有节点 / 注册服务 / 按 nodeId 获取服务 / 获取所有节点的指定服务 |
| `cluster/ServerNode.java` | **新建** — 集群节点描述 |
| `cluster/support/HazelcastClusterManager.java` | **新建** — IMap 节点注册表 + 本地服务注册表 + 远程动态代理 + 成员退出清理 |
| `cluster/support/task/RemoteServiceInvoker.java` | **新建** — 远程调用器（调用方：提交任务 + 超时/异常处理） |
| `cluster/support/task/RemoteInvokeTask.java` | **新建** — 远程调用任务（目标节点反射执行 + Spring 传输类载体） |
| `cluster/config/HazelcastClusterAutoConfiguration.java` | **新建** — HazelcastInstance + ClusterManager bean |
| `cluster/config/ClusterProperties.java` | **新建** — 集群配置项 |
| `sa-base/pom.xml` | 添加 `com.hazelcast:hazelcast:5.3.7` |

> 包 `cluster/` 是通用顶层包，与业务模块（`device/` 等）平级、无依赖 — 保证通用性，后续任何业务都可注册服务进来。
> **DDD 分层（对齐 device 模块风格）**：根包放领域抽象（`ClusterManager` 接口 + `ServerNode` 模型，不依赖任何具体技术）；`support/` 放基础设施实现（Hazelcast 适配器 + 远程任务）；`config/` 放装配。

## 十一、验证方案

1. **编译**：`mvn compile -pl sa-base -o -q`
2. **节点管理验证**：两个实例组集群 → `getCurrentNodeId()` 各自返回本节点 ID；`getCurrentNode()` 返回本节点 ServerNode（host/注册时间等）；`getClusterNodes()` 各返回 2 个节点；`getServerNode(对方nodeId)` 返回对应节点；`getServerMember(对方nodeId)` 返回实时成员（可提交远程任务）
3. **服务注册/获取验证**：两节点各 `register(测试接口.class, 实现)` → 各节点 `getService(对方nodeId, 测试接口.class)` 返回可用实例（本地真实、远程代理）
4. **远程调用验证**：节点 A 调用节点 B 上的服务方法 → B 执行（日志可见）→ 结果回传 A；带 Mono 返回值的方法正常 block 回传
5. **业务异常透传验证**：目标节点方法抛 `BusinessException` → 调用方还原为原业务错误
6. **节点退出验证**：节点 B 宕机 → A 的 `getClusterNodes()` 变为 1 个；再调 B 的服务报"目标节点不可用"
7. **单机验证**：`enabled=false` 启动 → 无 Hazelcast 相关 bean，应用正常
8. **全节点服务获取验证**：两节点各 `register(测试接口.class, 实现)` → 各节点 `getServices(测试接口.class)` 返回 2 个服务（本节点真实实例 + 远程代理），逐个调用正常
